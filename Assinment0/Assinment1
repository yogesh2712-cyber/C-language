/*1.Check if number is Even or Odd*/
#include <stdio.h>
int main() {
    int num = 7;
    if (num % 2 == 0)
        printf("%d is Even\n", num);
    else
        printf("%d is Odd\n", num);
    return 0;
}
